import { DEFAULT_AVAILABLE_TOOLS } from "@crow-central-agency/shared";

/** Set of Claude Code builtin tool names for quick lookup */
export const BUILTIN_TOOL_SET = new Set<string>(DEFAULT_AVAILABLE_TOOLS);
